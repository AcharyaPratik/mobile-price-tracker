# Direct dependencies only.
# Transitive packages (numpy, certifi, urllib3, etc.) are resolved by pip.
beautifulsoup4>=4.12
lxml>=5.0
pandas>=2.2
playwright>=1.40
psycopg2-binary>=2.9
python-dotenv>=1.0
requests>=2.31
